# SUMUS QUIZ BATTLE V0.9.2A QA REPORT

검증일: 2026-08-28 (Asia/Seoul)  
Production 기준점: `5476b0bd4243859860296c8f9fb8e16dc3ee94b7`  
대상: V0.9.2A VISUAL CLEANUP candidate (push/deploy 전)

## 1. 결과 요약

- Phase 0 regression baseline: PASS
- Unit/data/protocol/registry: 11/11 PASS
- Production client build: PASS, 1,173,491 bytes
- 24-client load test: PASS
- Classroom E2E: 3/3 PASS
- Visual QA: RUN 1/8/20/24 PASS, mobile 390x844 및 360x800 PASS
- Browser page errors: 0
- GitHub push: 실행하지 않음
- Render deploy: 실행하지 않음

## 2. Phase 0 baseline 복구

`test/e2e-classroom.test.js`가 browser lexical `BattleSession`을 `page.evaluate()`에서 직접 참조하던 부분을 기존 public audit interface인 `window.SUMUS_TEACHER_AUDIT.snapshot()`으로 교체했다. 제품 객체를 `window`에 추가 노출하지 않았다.

같은 E2E에 stale empty setup room 회귀 검사를 추가했다. 새 battle을 만든 뒤 빈 setup 상태로 돌아가 새로고침했을 때 HOME이 유지되고 `restoreApplied()`가 `false`인지 검증한다.

최종 classroom E2E는 다음 흐름을 실제 teacher/student browser로 검증한다.

1. CREATE BATTLE 및 individual range 구성
2. 학생 2명 입장, 학생 본인의 범위 번호 선택, character 및 READY
3. 일반 `START BATTLE` 실행
4. 학생 A 선택지 제출, 학생 B PASS, 두 학생 모두 다음 문제 진입
5. PAUSE 및 RESUME
6. 학생 reconnect 후 player identity 유지
7. 교사 paused 상태 refresh recovery 및 전체 state 비교
8. RESUME, FINISH, results 진입

## 3. 자동 테스트 결과

| Gate | 결과 | 핵심 수치 |
|---|---:|---|
| `npm test` | PASS | 11/11 |
| `npm run build` | PASS | 1,173,491 bytes |
| `npm run test:load` | PASS | 24 join/ready/answers, 5 reconnect, collision rejected |
| `npm run test:e2e` | PASS | 3/3 |
| `npm run test:visual` | PASS | 1/8/20/24, 390x844, 360x800 |

Load test 관측값: lookup 평균 8ms, p95 12ms, answer batch 12ms, pause/resume broadcast 1ms, RSS 58.3MB.

## 4. Teacher / RUN visual QA

모든 teacher 캡처와 DOM 측정은 1920x1080 production 쿼리에서 실행했다. 합성 학생은 실제 학생 UI heartbeat를 실행하지 않으므로 기존 production FORCE START로 renderer를 만든 뒤 `?role=teacher`를 새로고침하여 recovery 상태에서 측정했다. 일반 START 동작은 별도의 classroom E2E에서 검증했다.

| 인원 | Runner clipping | Nameplate clipping | Nameplate overlap pair | Ranking rows | Production QA controls |
|---:|---:|---:|---:|---:|---|
| 1 | 0 | 0 | 0 | 1 | hidden |
| 8 | 0 | 0 | 0 | 8 | hidden |
| 20 | 0 | 0 | 0 | 20 | hidden |
| 24 | 0 | 0 | 0 | 24 | hidden |

Production 로비에서 FORCE START, ADD DEMO PLAYERS, CLEAR, COPY STUDENT URL이 모두 표시되는지 확인했다. build tag, AUTO DEMO, DEMO SPEED, OPEN STUDENT TEST, debug panel은 숨김 상태로 확인했다. RUN의 PAUSE와 FINISH도 유지했다.

정량 결과 원본: `qa/v0.9.2a/visual-metrics.json`

## 5. Student mobile visual QA

| Viewport | Shell | 4 answer buttons | PASS | Build/debug UI | Page error |
|---|---|---|---|---|---:|
| 390x844 | viewport 내부 | 모두 내부 | 내부 | hidden | 0 |
| 360x800 | viewport 내부 | 모두 내부 | 내부 | hidden | 0 |

390x844에서 question hierarchy, answer button, PASS, wrong feedback overlay를 별도로 캡처했다. 피드백 시간과 question lifecycle은 변경하지 않았다.

## 6. Before / after evidence

정확한 baseline으로 확보된 HOME 1920x1080과 RUN 1920x1080을 before evidence로 유지했다. 초기 in-app browser에서 얻은 모바일 baseline은 실제 viewport가 1280x720이어서 비교 근거에서 제외하고 `qa/v0.9.2a/exploratory/`에 격리했다.

- Before: `qa/v0.9.2a/before-teacher-home-1920x1080.png`
- After: `qa/v0.9.2a/after-teacher-home-1920x1080.png`
- Before RUN: `qa/v0.9.2a/before-run-10-1920x1080.png`
- After RUN: `qa/v0.9.2a/after-run-{1,8,20,24}-1920x1080.png`
- After results: `qa/v0.9.2a/after-teacher-results-1920x1080.png`
- After student: `qa/v0.9.2a/after-student-question-{390x844,360x800}.png`
- After feedback: `qa/v0.9.2a/after-student-feedback-390x844.png`

## 7. Production freeze 확인

다음 파일과 영역은 수정하지 않았다.

- `server.js`, `lib/protocol.js`, `lib/room-registry.js`
- `client/network.js`, `client/student.js`, `client/teacher.js`, `client/range.js`, `client/data.js`
- `data/*.json`, `hotfix_v081~v084`, `bundle/part*.txt`
- `QuizEngine`, `ScoreEngine`, `TeacherBridge` gameplay behavior, `StudentApp` gameplay behavior
- `RaceEngine.answer`, `RaceEngine.frame`, `RaceEngine.finish`

V0.9.2A layer에는 CSS, visual class, `data-*` decoration, debug visibility gating만 있다. 정적 테스트가 금지된 engine method override를 검사한다.

## 8. V0.9.2B 이관 항목

- RUN motion cadence, runner motion polish, camera/interpolation은 V0.9.2B에서 별도 검토
- 실제 수업의 다양한 이름 길이/한글·영문 혼합에 대한 projector 현장 확인
- 20~24명 레이스 중간·후반 progress 분산 상태의 motion QA
- 시작선/결승선 진입 연출과 sound/motion 동기화
- V0.9.2C 대상인 full podium animation은 이번 버전에서 제외
