# SUMUS QUIZ BATTLE V0.9.3C.3 QA Report

## 결과

RAID Student와 RAID Teacher를 RUN DOM에서 분리된 전용 Shell로 재구축했다. 서버, room registry, WebSocket, reconnect, question/scoring/gameplay domain은 공유하지만 화면 구조와 연출은 공유하지 않는다. RUN 화면·모션·서버 프로토콜은 기준 커밋 `a7aaad1` 대비 보존했다.

## 수정 파일

- `client/raid-shell.js` — 전용 Student/Teacher/Lobby Shell과 GAME MODE 선택기
- `client/raid-presentation.js` — 전투 카메라, 실제 에셋, FX, Teacher broadcast, event/ranking
- `client/raid-premium.css` — RAID 범위 한정 responsive UI
- `client/raid-mode.js` — 기존 RUN 위에 얹던 legacy RAID HUD/CSS 제거 및 전용 presentation 연결
- `client/data.js`, `hotfix_v084_public_stable.js` — same-origin/canonical 학생 URL
- `lib/build-client.js` — RAID Shell bundle 순서
- `test/build.test.js`, `test/raid-presentation.test.js` — 구조·origin·24 grid·viewport 회귀 검증
- `RAID_VISUAL_PREVIEW.html`, `README.md` — 결과 프리뷰와 문서
- `qa/v0.9.3c3/*` — 실제 runtime 캡처와 측정값

## Shell 분리

- RUN: 기존 `StudentApp.render`와 `#race` 화면을 그대로 사용한다.
- RAID Student: `renderRaidStudentShell()`이 `.raid-student-shell`을 직접 생성하며 RUN의 `.student-shell`, race HUD, `LOCAL DEMO CLIENT`를 마운트하지 않는다.
- RAID Teacher: `renderRaidTeacherShell()`이 `#raidTeacherScreen`을 별도 sibling screen으로 생성하고 `#race`를 비활성화한다.
- RAID Lobby: `#raidLobbyScreen` 전용 초대/파티 화면을 사용한다. RUN lobby 문구와 트랙 연출은 노출하지 않는다.
- 공통 영역은 battle/session data, server-authoritative timing, room registry, WebSocket, reconnect, question/scoring/gameplay domain뿐이다.

## Student 구조

`100dvh` 4영역 grid: 상단 통합 상태 HUD, 비대칭 전투 stage, 문제/ATTACK TIME, safe-area bottom action. Player는 좌하단, enemy는 우측이며 boss는 더 크게 경계를 넘도록 배치한다. 선택지는 RAID Shell에서 1–4로 표기하고 angular frame을 적용한다. Damage/critical FX는 enemy hit point 인근의 presentation-only layer에 생성되며 combo 3/5/10/15/20 단계가 레이아웃 이동 없이 강화된다.

## Teacher 구조

16:9 고정 broadcast console: 상단 LIVE/control bar, 좌측 invite+AUTO SPOTLIGHT, 우측 ALL STUDENTS, 하단 최근 5 event+top 5 ranking. Spotlight는 Teacher 전용 카메라이며 문제·정답은 노출하지 않는다. Ranking은 progress, clear, accuracy, max combo를 사용하고 damage를 학업 순위 기준으로 쓰지 않는다.

## 실제 RAID 에셋

`RaidAssetRegistry`/`RaidAssetDirector`를 통해 다음 실제 WebP/PNG를 사용한다.

- Player: `raid-player-main-{idle,attack,critical,hit,shield,down,revive,special}`
- Common: `raid-common-{idle,attack,hit,clear}`
- Elite: `raid-elite-{idle,attack,hit,guard,clear}`
- Boss: `raid-boss-{idle,attack,hit,guard,heavy-charge,clear}`
- FX: `raid-fx-{hit-normal,hit-weak,hit-critical,shield,guard,guard-break,perfect-break,player-hit,heavy-hit,heavy-charge,heavy-cancel}`

캐릭터 emoji, 원형 placeholder avatar, generic monster는 사용하지 않는다.

## 24명 Grid

Teacher roster는 `repeat(4) × repeat(6)` CSS grid이며 paging/scroll 없이 24개를 표시한다. 실제 24 WebSocket client로 1366×768, 1600×900, 1920×1080에서 `24/24 visible`, document overflow 0, RUN screen inactive, answer exposure false를 확인했다.

## Public QR Origin

과거 `sumus-quiz-battle-public-v083.onrender.com` 하드코딩을 제거했다. `SUMUS_CANONICAL_PUBLIC_ORIGIN`이 있으면 우선 사용하고, 아니면 `window.location.origin`을 사용한다. 학생 링크에는 `role=student`, 5자리 `code`, `mode=raid`가 포함되며 Teacher와 동일 room registry origin을 사용한다. localhost fallback은 file/local debug에만 남겼다.

## Responsive QA

| 화면 | 결과 |
|---|---|
| Student 360×667 | overflow 0, answer 4개 visible, PASS bottom 660, RUN header 없음 |
| Student 390×844 | overflow 0, PASS/answers/safe area visible |
| Student 412×915 | overflow 0, PASS bottom 914 |
| Student 430×932 | overflow 0, PASS bottom 931 |
| Teacher 1366×768 | 24/24 visible, 4×6, overflow 0 |
| Teacher 1600×900 | 24/24 visible, 4×6, overflow 0 |
| Teacher 1920×1080 | 24/24 visible, 4×6, overflow 0 |

정량값: `qa/v0.9.3c3/raid-shell-metrics.json`.

## RUN Regression

- `npm run test:visual`: PASS — RUN 1/8/20/24, 390×844, 360×800, overlap 0, page error 0.
- `npm run test:motion`: PASS — lobby/start/final sprint/results motion flow.
- 기준 커밋 대비 `client/run-motion.js`, `client/premium-director.js`, `client/raid-domain.js`, `server.js`, `lib/room-registry.js` 변경 없음.

## RAID 기능/실데이터 QA

- 1/8/20/24명 room join과 준비/시작을 검증했다.
- answer, wrong, PASS, timeout, down, revive, boss guard/heavy, clear 상태는 기존 gameplay/domain 테스트로 검증했다.
- server restart 후 Teacher/Student가 진행 중 RAID question으로 재접속되는 것을 실제 브라우저에서 확인했다.
- `RaidStagePlanner`의 최대 68문항 transition과 2–6 wave 계획을 기존 테스트로 확인했다.
- Gameplay 수치와 protocol/domain은 변경하지 않았다.

## Test 결과

- `npm test`: PASS, 36/36
- `npm run build`: PASS, bundle 1,320,610 bytes
- `npm run test:e2e`: PASS, 3/3
- `npm run test:load`: PASS — 24 clients/answers, reconnect 5, collision reject, lookup avg 8ms/p95 10ms, memory 59.2MB
- `npm run test:visual`: PASS
- `npm run test:motion`: PASS

## Known limitations

- 교사 1366×768의 24명 카드 텍스트는 프로젝터 거리보다 화면 밀도를 우선한 최소 크기다. 1600×900 이상에서 가독성이 더 좋다.
- 브라우저 TTS 품질과 음성 선택은 OS/browser speech synthesis 구현에 의존한다.
- 실제 네트워크 지연/프로젝터 색 재현은 로컬 QA 범위를 벗어난다.

## Screenshot / Preview

- `qa/v0.9.3c3/student-raid-360x667.jpg`
- `qa/v0.9.3c3/teacher-raid-24-1366x768.jpg`
- `qa/v0.9.3c3/teacher-raid-24-1920x1080.jpg`
- `RAID_VISUAL_PREVIEW.html`

## Commit

최종 구현 commit: `c893d2f45601b8aae429a9a95af32a65eb7d7654`
