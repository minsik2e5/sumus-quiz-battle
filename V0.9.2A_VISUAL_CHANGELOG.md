# SUMUS QUIZ BATTLE V0.9.2A VISUAL CHANGELOG

Production 기준점: `5476b0bd4243859860296c8f9fb8e16dc3ee94b7`

## Presentation architecture

- `client/visual-cleanup.js`를 독립 presentation-only layer로 추가
- `lib/build-client.js`의 `HOTFIX_FILES` 마지막에 삽입
- `window.SUMUS_VISUAL_BUILD` public visual audit marker 추가
- DOM mutation은 visual `class`/`data-*` decoration에만 사용하고 runner style attribute는 관찰하지 않음

## Debug / QA isolation

- production 기본 화면에서 build/commit tag, AUTO DEMO, DEMO SPEED, disconnect/reconnect debug panel, OPEN STUDENT TEST, student lab/preview 도구 숨김
- FORCE START, ADD DEMO PLAYERS, CLEAR, COPY STUDENT URL은 기존 production 운영 기능으로 유지
- `.student-lobby-tools` 컨테이너는 유지하고 그 안의 `#openStudentTest`만 선택적으로 숨김
- 삭제 없이 `?debug=1`에서 기존 도구 복원
- reconnect 기능과 WebSocket 동작은 변경하지 않음

## Global design cleanup

- dark navy surface, mint/teal primary, champion gold, success/danger/warning token 정리
- panel, button, card, selected state, typography, spacing hierarchy 통일
- 과도한 glow를 줄이고 border/surface 대비 중심으로 정리
- HOME의 CREATE BATTLE을 색상과 surface contrast로 명확한 primary action으로 표현

## RUN readability

- HUD progress, leader, LIVE badge와 mini ranking 대비 강화
- track/background 분리, start marker, finish gate/scoreboard 존재감 강화
- runner shadow와 nameplate의 surface, border, font hierarchy 개선
- 1/8/20/24명 구간별 visual scale class 적용
- 1위 nameplate와 ranking 1/2/3위 색상 hierarchy 적용
- production race footer에서 gameplay PAUSE/FINISH를 유지하고 QA controls 숨김

## Student question UI

- HUD 밀도 축소, prompt와 answer hierarchy 강화
- A/B/C/D key pill, touch target, answer surface polish
- PASS를 warning 색상의 full-width action으로 구분
- correct/wrong/pass feedback surface를 단순화하고 읽기 쉬운 solid/radial hierarchy 적용
- lifecycle, feedback timeout, submission/attempt timing은 변경하지 않음

## Results presentation

- champion gold, 2위 silver, 3위 bronze hierarchy 적용
- podium과 award card surface 정리
- teacher result table의 1위 행 강조
- student result에서 핵심 rank/score 영역을 우선 강조
- awards 계산과 podium animation sequence는 변경하지 않음

## Regression / QA support

- E2E의 lexical `BattleSession` 직접 접근을 `SUMUS_TEACHER_AUDIT`로 교체
- stale empty setup room auto-restore 차단 직접 regression 추가
- classroom E2E에 선택지 제출과 PASS 경로 포함
- `scripts/visual-qa.js` 및 `npm run test:visual` 추가
- exact viewport screenshot, clipping, nameplate overlap, debug visibility, browser error 자동 검증

## Explicitly unchanged

- WebSocket, Battle Code, room registry, reconnect/recovery, range system
- datasets 및 practice localStorage
- scoring, distance, rank, finish, camera, interpolation
- question lifecycle와 awards 계산
