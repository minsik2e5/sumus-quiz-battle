f=window.saveVocabRecord;window.saveVocabRecord=function(){const r=f.apply(this,arguments);const row=arguments[3]?.closest?.('.vocab-row'),b=row?.querySelector('.vocab-status');if(b){b.textContent='응시 ✓';b.classList.remove('missing','absent');b.classList.add('taken')}return r}}
// 최근 시험 요약에 미입력 인원도 표시
if(typeof window.renderVocabRecent==='function'){window.renderVocabRecent=function(){const cls=document.getElementById('vocabCls')?.value||classes()[0];const tests=Object.values(S.vocabTests||{}).filter(t=>t.cls===cls&&Object.keys(t.records||{}).length).sort((a,b)=>(b.date||'').localeCompare(a.date||'')).slice(0,8);const all=(S.students[cls]||[]).filter(s=>getStudentOptions('english',cls,s.name).vocab!==false);const el=document.getElementById('vocabRecent');if(!el)return;el.innerHTML=tests.length?tests.map(t=>{const recs=Object.values(t.records||{}),scores=recs.filter(r=>r.status!=='미응시'&&typeof r.score==='number').map(r=>r.score),avg=scores.length?Math.round(scores.reduce((a,b)=>a+b,0)/scores.length):'-',abs=recs.filter(r=>r.status==='미응시').length,missing=Math.max(0,all.length-recs.filter(r=>r.status==='미응시'||typeof r.score==='number').length);return `<div class="student-item"><div class="s-avatar">${t.type==='vocab'?'단':'시'}</div><div class="s-info"><div class="s-name">${esc242(t.date)} · ${esc242(t.title)}</div><div class="s-sub">${t.type==='vocab'?'단어':'테스트'} · 평균 ${avg}점 · 미응시 ${abs}명 · 미입력 ${missing}명</div></div></div>`}).join(''):`<div class="empty"><div class="empty-text">기록 없음</div></div>`}}
// Profile: 위쪽 출석 stats와 중복되는 '출석률' 카드를 '최근 메모'로 교체
if(typeof window.renderProfileEnglish==='function'){const f=window.renderProfileEnglish;window.renderProfileEnglish=function(