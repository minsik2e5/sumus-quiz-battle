div><div class="mobile-hide"></div><div class="vocab-score">-</div><div><span class="badge neutral">해당없음</span></div></div>`;const rec=view.records?.[name];const [lab,cl]=statusLabel(rec,type);
    if(type==='vocab'){const score=rec?.status==='미응시'?null:(rec&&hasVocabInput(rec,type)?vocabScoreFromWords(rec.totalWords,rec.wrongWords):null);return `<div class="vocab-row"><div class="s-avatar">${esc242(name.slice(0,1))}</div><div><div class="s-name">${esc242(name)}</div><div class="s-sub">${esc242(grade||'')}</div></div><input class="vocab-input" type="number" min="0" value="${rec?.totalWords??''}" oninput="saveVocabRecord('${name}','totalWords',this.value,this)"><input class="vocab-input" type="number" min="0" value="${rec?.wrongWords??''}" oninput="saveVocabRecord('${name}','wrongWords',this.value,this)"><div class="vocab-score">${rec?.status==='미응시'?'미응시':score!==null?score+'점':'-'}</div><button class="btn btn-outline btn-sm vocab-status ${cl}" onclick="toggleVocabAbsent('${name}')">${lab}</button></div>`}
    const max=rec?.maxScore||view.maxScore||100,score=rec?.status==='미응시'?null:(rec&&hasVocabInput(rec,type)?vocabScoreFromTest(rec.rawScore,max):null);return `<div class="vocab-