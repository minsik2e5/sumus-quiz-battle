# SUMUS QUIZ BATTLE V0.9.3B.1 FINAL RAID ASSET DROP QA REPORT

검수일: 2026-09-01 (Asia/Seoul)

## 1. Baseline commit

- V0.9.3B 기준: `5f7d76e6425e39724215b0bb65b8bd72174118cc`
- V0.9.3B의 기준 commit: `beb522501e2d514c8a64522e5b488a1c19b831f9`
- 작업 전 production `main`: `d52283c8239302586f1eb63160937107f1218465`

## 2. Branch

`v0.9.3b1-final-raid-asset-drop`

## 3. Final commit

기능·에셋 통합 커밋과 보고서 커밋을 포함하는 위 branch의 최종 HEAD가 검수 기준이다. 정확한 HEAD hash는 최종 handoff 메시지에 함께 기록한다.

## 4. Asset ZIP 확인

`SUMUS_RAID_ASSETS_FOR_CODEX (1).zip`을 별도 디렉터리에 완전 해제하고 `manifest.json`, `README_ASSET_MAP.md`, `ASSET_MAP.csv`, canonical 34개, `_pending_review` 4개, `_reference` 5개를 확인했다. `_pending_review`와 `_reference`는 runtime에 복사하지 않았다. 원본 provenance 문서 3개는 `assets/raid/`에 보존했다.

## 5. Canonical asset count

총 34개: PLAYER 8, COMMON 4, ELITE 5, BOSS 6, 확정 FX 11. 누락 0, 0-byte 0, PNG alpha/RGBA 및 IHDR 구조 검사 통과.

## 6. Converted WebP count

34개 전부 high-quality WebP로 변환했다. 투명도와 원본 해상도를 유지했다. PNG 합계 66,445,814 bytes에서 WebP 16,043,954 bytes로 줄어 원본 대비 24.1%다. PNG 원본도 삭제하지 않았다.

## 7. Missing asset

Canonical 누락은 0개다. 아직 확정되지 않은 FX 4개는 manifest에 `pendingFallbackOnly`로 명시했다.

## 8. Fallback behavior

공통 asset director가 `WebP → PNG → 기존 SVG/CSS` 순서로 시도한다. 외부 URL은 없다. 브라우저의 `FALLBACK CHECK`에서 WebP/PNG 경로를 의도적으로 깨뜨렸을 때 PLAYER, COMMON, teacher PLAYER/BOSS의 4개 대체 표현이 렌더링됐고 문제/게임 진행 의존성은 없었다.

## 9. Player mapping

IDLE, ATTACK/WEAK, CRITICAL, HIT, SHIELD, DOWN, REVIVE, SPECIAL을 각각 승인된 8개 PLAYER canonical asset에 연결했다. PLAYER는 왼쪽에 고정하고 responsive `clamp()` 크기를 적용했다.

## 10. Common mapping

COMMON wave의 IDLE, ATTACK, HIT, CLEAR를 4개 canonical asset에 연결했다. tier 중 가장 작은 scale을 사용한다.

## 11. Elite mapping

ELITE wave의 IDLE, ATTACK, HIT, GUARD, CLEAR를 5개 canonical asset에 연결했다. GUARD는 `raid-elite-guard`를 사용한다.

## 12. Boss mapping

BOSS wave의 IDLE, ATTACK, HIT, GUARD, HEAVY CHARGE, CLEAR를 6개 canonical asset에 연결했다. BOSS는 가장 큰 responsive scale을 사용한다.

## 13. FX mapping

NORMAL/WEAK/CRITICAL HIT, SHIELD, GUARD, GUARD BREAK, PERFECT BREAK, PLAYER HIT, HEAVY HIT/CHARGE/CANCEL의 확정 FX 11개를 asset director와 presentation FX layer에 연결했다. WEAK < NORMAL < CRITICAL 크기 규칙을 유지하고 문제 UI 및 teacher cards를 침범하지 않게 제한했다.

## 14. Pending FX

`raid-fx-revive`, `raid-fx-word-tamed`, `raid-fx-special`, `raid-fx-combo-aura`는 승인 전 후보를 승격하지 않고 기존 CSS/SVG fallback을 유지했다. Arena/UI 최종 artwork도 pending이다.

## 15. Student visual QA

실제 브라우저에서 390×844 및 360×800을 검사했다. COMMON/ELITE/BOSS와 PLAYER idle/attack/critical/hit/shield/down/revive/special, elite/boss guard, boss attack/hit/heavy/clear 상태의 projection과 asset 선택을 점검했다. 가로 overflow 0, canonical broken image 0, 질문과 4개 선택지 및 PASS 가독성 정상이다. 디버그 REVIVE 미리보기의 중복 overlay 문구도 제거했다.

## 16. Teacher visual QA

1280×720 및 1440×900, 24명 compact cards를 실제 브라우저로 검증했다. 카드 겹침 0, 이름 overflow 0, 페이지 가로 overflow 0, spotlight asset load 정상, mini card full animation 없음, 학생 정답/입력 노출 없음. AUTO SPOTLIGHT 판단 코드는 변경하지 않았다.

## 17. 20-question QA

엔진 테스트에서 자동 stage planner가 COMMON → ELITE → BOSS wave를 완료했다. total HP, damage, combo 및 snapshot 상태 누락이 없었다. 상태별 asset 교체는 visual projection/browser matrix에서 확인했다.

## 18. 68-question QA

하드코딩 없는 planner가 68문제를 처리했고 모든 tier 전환, 누적 HP/damage/combo, 반복 snapshot serialization이 통과했다. 장시간 visual projection도 DOM/gameplay source를 변경하지 않았다.

## 19. 24-player QA

부하 결과: clients 24, ready 24, answers 24, teacher players/cards 24, reconnects 5. collision rejection 정상, lookup 평균 8ms/P95 14ms, answer batch 18ms였다.

## 20. Reconnect QA

엔진 snapshot 테스트가 HP, enemy HP, combo, wave, shield, guard, heavy, revive, question/deadline 복원을 검증했다. 부하 테스트에서 5명 reconnect가 성공했다. 실제 브라우저에서도 server restart 뒤 학생과 교사가 기존 RAID room/state로 자동 복귀하고 현재 state asset이 다시 선택됐다.

## 21. Pause/resume QA

기존 pause/resume gameplay 로직은 변경하지 않았다. 24명 부하 테스트에서 pause/resume broadcast 2ms, 상태 유지 및 재개 정상. presentation은 authoritative snapshot만 읽으며 deadline을 쓰지 않는다.

## 22. RUN regression

`client/run-motion.js`, `client/premium-director.js`, RUN gameplay/scoring/ranking/finish/reconnect/final sprint 코드는 baseline 대비 변경 0이다. Visual QA는 1/8/20/24명, 390×844/360×800에서 overlap 0/page error 0으로 통과했다. Motion QA는 READY/3/2/1/GO, run cycle, boost, rank, final sprint, finish, podium을 통과했고 `V0.9.2B` Motion 및 `V0.9.2B.1` Premium Director marker를 유지했다.

## 23. Performance

- Build: 1,284,392 bytes, PASS
- Unit/engine/presentation/asset: 34/34 PASS
- E2E: PASS
- Load memory RSS: 62.1 MB
- WebP payload: 원본 PNG 대비 24.1%
- Preload: RAID core asset을 module load 시, 다음 visual/enemy 묶음을 state projection 시 비동기 preload한다. 실패는 gameplay를 block하지 않는다.

## 24. Known issues

- REVIVE, WORD TAMED, SPECIAL, COMBO AURA의 최종 FX가 미확정이므로 승인된 기존 fallback 표현을 사용한다.
- Arena/UI 최종 artwork는 이번 범위가 아니며 현재 presentation을 유지한다.
- 자동 motion QA가 기록한 resource 404 두 건은 기존 선택적 리소스이며 테스트 정책상 무시 대상이다. RAID canonical asset 누락은 아니다.
- 브라우저 자동화 보안 정책 때문에 `file://` 직접 열기 대신 동일 상대 경로의 preview를 임시 localhost에서 검증했다. HTML 자체는 외부 의존성이 없어 로컬 파일로 실행 가능하다.

## 25. Screenshots

`qa/v0.9.3b1/`에 다음 증적을 저장했다.

- `raid-player-actual-390x844.png`
- `raid-common-actual-390x844.png`
- `raid-elite-actual-390x844.png`
- `raid-boss-actual-390x844.png`
- `raid-boss-heavy-390x844.png`
- `raid-player-down-390x844.png`
- `raid-player-revive-390x844.png`
- `raid-player-revive-360x800.png`
- `raid-common-actual-360x800.png`
- `raid-boss-actual-360x800.png`
- `raid-teacher-24-1280x720.png`
- `raid-teacher-24-1440x900.png`
- `raid-visual-preview-1440x900.png`
- `run-regression-24.png`
- 수치 증적: `raid-asset-metrics.json`

## 26. Production unchanged

Production checkout `main`은 작업 전후 `d52283c8239302586f1eb63160937107f1218465`, clean 상태다. merge, deploy, production write, push를 하지 않았다.

## Executed commands

- `npm test` — 34/34 PASS
- `npm run build` — PASS
- `npm run test:e2e` — PASS
- `npm run test:load` — PASS
- `npm run test:visual` — PASS
- `npm run test:motion` — PASS
- 실제 브라우저 RAID student/teacher/state/fallback QA — PASS
