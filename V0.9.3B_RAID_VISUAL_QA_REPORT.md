# V0.9.3B RAID VISUAL & ASSET QA REPORT

## 1. Baseline commit

- V0.9.3A.1 기준: `beb522501e2d514c8a64522e5b488a1c19b831f9`
- 작업 전 `origin/main`: `5c3b02f2275571fc73b05c8ac3d55e8504de4b90`
- RUN Motion, Premium Director, RAID domain 파일은 기준 commit과 byte-level diff가 없다.

## 2. Branch

- `v0.9.3b-raid-visual-asset-pass`
- merge/deploy하지 않았으며 검수 가능한 개발 branch로 유지한다.

## 3. Modified files

- build/QA: `lib/build-client.js`, `package.json`, `test/build.test.js`, `.github/workflows/v092b-qa.yml`
- asset registry: `assets/manifest.json`, `assets/raid/manifest.json`, `assets/raid/*/.gitkeep`
- RAID presentation integration: `client/raid-mode.js`
- 신규 presentation/asset/model 모듈 및 QA artifact

`client/raid-mode.js` 변경은 presentation이 장식한 DOM을 동일 markup 재렌더가 지우지 않도록 마지막 markup을 기억하는 2줄의 렌더 안정화뿐이다. HP, damage, combo, timer, wrong, PASS, stage, revive, GUARD, HEAVY, spotlight 판정은 변경하지 않았다.

## 4. New visual modules

- `client/raid-visual-model.js`: gameplay snapshot을 변경하지 않고 화면용 tier, arena, timer phase, primary state를 투영한다.
- `client/raid-assets.js`: 로컬 asset slot, 로드 성공/실패 전환, original SVG fallback을 담당한다.
- `client/raid-presentation.js`: 학생 전투 stage, HUD, combat feedback, 교사 broadcast spotlight/card/feed를 담당한다.

Build 순서는 RUN Motion → Premium Director → RAID domain → RAID mode → RAID assets → RAID visual model → RAID presentation이다.

## 5. Asset architecture

`assets/raid/characters`, `monsters`, `arenas`, `fx`, `ui`를 replaceable boundary로 추가했다. player, COMMON/ELITE/BOSS, arena, hit/critical/shield/guard/heavy/player-hit/revive/clear FX ID가 registry에 등록된다. 모든 URL은 repository-local이며 external hotlink는 0건이다.

## 6. Fallback behavior

PNG/WebP가 있으면 해당 asset을 사용하고, 누락/로드 실패 시 inline original SVG 또는 CSS FX를 표시한다. 실제 브라우저 QA는 최종 artwork가 없는 상태에서 fallback 2개가 정상 표시되고 레이아웃이 유지됨을 확인했다.

## 7. Student UI changes

NAVY/GOLD identity 위에 side-view player-versus-monster stage, wave/tier/monster HP, compact player HP/combo/shield, MONSTER ATTACK GAUGE, readable question/choice/PASS panel을 구성했다. COMMON은 mint/cyan, ELITE는 purple/silver, BOSS는 gold/magenta/violet 조명으로 구분한다.

## 8. Teacher UI changes

AUTO SPOTLIGHT logic은 유지하면서 한 명의 battle을 크게 보여주는 broadcast scene, name bumper, 24개 lightweight status card, 최근 event feed로 재구성했다. 카드에는 번호, 이름, HP, enemy progress, combo, state만 표시한다. 학생 문제와 정답/입력 내용은 spotlight에서 노출하지 않는다.

## 9. Animation states

IDLE, PLAYER ATTACK, CRITICAL, NORMAL, WEAK, WRONG, PASS, SHIELD, GUARD, GUARD BREAK/PERFECT BREAK, HEAVY ATTACK/CANCELLED, PLAYER HIT, DOWN, REVIVE, WORD TAMED, BOSS CLEAR 및 wave/tier 전환 presentation을 구현했다. effect는 gameplay 진행을 block하지 않으며 기본 950ms 뒤 제거된다.

## 10. Performance considerations

- full battle animation은 학생 자신의 1개 scene과 교사 spotlight 1개에만 적용한다.
- 24개 학생 카드는 static/lightweight이며 canvas나 continuous particle system을 사용하지 않는다.
- effect node는 최대 6개로 제한하고 timeout cleanup한다.
- `prefers-reduced-motion`에서 shake, flash, 반복 motion을 제거/단축한다.
- presentation test가 frozen gameplay field 직접 assignment를 거부한다.

## 11. 390x844 results

COMMON normal, CRITICAL, SHIELD, GUARD, HEAVY ATTACK, player hit, DOWN, REVIVE, BOSS, BOSS CLEAR를 실제 브라우저로 캡처했다. 가로 overflow 0, 4개 choice 가독성 정상, monster/HUD/question 겹침 0, PASS 접근 가능, attack gauge 상태 구분 정상이다.

## 12. 360x800 results

normal 및 BOSS를 캡처했다. 가로 overflow 0이며 question, 4개 choice, PASS가 360px 폭에서 정상 동작한다. BOSS silhouette과 HUD가 문제 panel을 가리지 않는다.

## 13. 24-player teacher results

- 1280x720: 24 cards, card overlap 0, name overflow 0, horizontal overflow 0, answer exposure 0.
- 1440x900: 동일 항목 통과, spotlight 폭 약 1002.5px.
- AUTO ON↔MANUAL, PAUSE↔RESUME를 실제 버튼으로 왕복 검증했다.
- event feed는 자체 제한/scroll container 안에 유지되어 page layout을 밀어내지 않는다.

## 14. 68-question visual endurance result

68개 projection을 연속 생성해 COMMON, ELITE, BOSS 전환, long combo, revive, GUARD, HEAVY 상태를 순회했다. source snapshot mutation 0건, projection serialization 정상, effect node cap 6, preview DOM 877~882 nodes, effect cleanup 950ms로 무제한 DOM 누적이 없음을 확인했다. gameplay의 68문제 engine endurance test도 HP/damage/combo/wave/snapshot 누적 상태를 통과했다.

## 15. RUN regression result

- RUN 1/8/20/24 visual regression: PASS, runner/nameplate stage 이탈 0, overlap 0.
- V0.9.2B Motion: READY→3→2→1→GO, run cycle, boost, dip, rank change, final sprint, finish, podium PASS.
- Premium Director `V0.9.2B.1`: PASS.
- `client/run-motion.js`, `client/premium-director.js`: baseline과 diff 0.
- 전체 명령: `npm test` 31/31, `npm run build`, `npm run test:e2e`, `npm run test:load`, `npm run test:visual`, `npm run test:motion` PASS.
- load: 24 ready/answer, reconnect 5, collision rejection PASS.

## 16. Known issues

- 최종 고품질 character/monster WebP artwork는 이번 범위가 아니므로 현재 QA는 original fallback SVG를 사용한다. registry slot에 파일을 추가하면 코드 변경 없이 교체 가능하다.
- 1920x1080 교사 RAID 캡처는 optional 항목으로 생략했다. 필수 1280x720 및 1440x900은 완료했다.
- event feed는 낮은 viewport에서 내부 스크롤을 사용한다. page-level horizontal overflow나 카드 겹침은 없다.

## 17. Screenshots

`qa/v0.9.3b/`에 학생 12장, 교사 2장, RUN 24인 회귀 1장과 `raid-visual-metrics.json`을 저장했다.

필수 artifact:

- `raid-student-normal-390x844.png`
- `raid-student-critical-390x844.png`
- `raid-student-guard-390x844.png`
- `raid-student-heavy-390x844.png`
- `raid-student-down-390x844.png`
- `raid-student-boss-390x844.png`
- `raid-teacher-24-1280x720.png`
- `raid-teacher-24-1440x900.png`
- `run-regression-24.png`
- `raid-visual-metrics.json`

## 18. Next recommended step

이 branch를 QA 기준본으로 검수한 뒤, 별도 승인 단계에서 `assets/raid` slot용 original WebP illustration을 제작·대입하는 것이 다음 단계다. gameplay balance, RUN redesign, merge, deploy는 이 작업에 포함하지 않는다.

## Production safety

별도 production checkout은 작업 후에도 clean이며 HEAD `d52283c8239302586f1eb63160937107f1218465`로 유지됐다. production main 변경, merge, deploy는 수행하지 않았다.
